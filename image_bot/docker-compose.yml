services:
  bot-backend:
    build: .
    container_name: image_bot_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./cache:/workspace/cache

networks:
  botnet:
    driver: bridge
